spring:
  application:
    name: ocpp-evse-backend
    
  datasource:
    url: jdbc:mysql://localhost:3306/ev_charging?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${SPRING_DATASOURCE_USERNAME:ocpp}
    password: ${SPRING_DATASOURCE_PASSWORD:ocpp123}
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

# JWT Configuration (NEW)
jwt:
  secret: aHR0cHM6Ly9naXRodWIuY29tL2FuaS1hbWF0aGVsc3Rhci9ldmNoYXJnaW5nLWFwaS1qd3Qtc2VjcmV0LWtleQ==
  expiration: 900000  # 15 minutes
  refresh-expiration: 604800000  # 7 days

server:
  port: 8080

logging:
  level:
    com.example.ocpp: DEBUG
    org.springframework.security: DEBUG